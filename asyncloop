ASYNC LOOP LEDGER
=================

Goal
- Reach staging/predeployment readiness with verified security, business-logic, and accounting safeguards.

Operating Rules
- After each completed step: update plan + append this file.
- Every code change must be committed.
- After each commit: run `codex review --commit <sha>` and log result here.
- Prioritize exploitable security flaws and accounting invariant violations first.

Current Loop Plan
- [in_progress] Harden configuration secrets and unsafe defaults in runtime profiles.
- [pending] Design and add abuse-driven tests for tenant/role boundary bypasses.
- [pending] Audit accounting posting and settlement flows for cross-module invariant gaps.
- [pending] Fix confirmed logic/security issues with minimal safe patches.
- [pending] Run targeted verification matrix (security + accounting + cross-module).
- [pending] Iterate through high-risk modules (inventory, purchasing, invoicing, payroll).

Commit Ledger
- 2026-02-12: `16b2a2c4`
  - Harden packing endpoints with explicit role gate.
  - Added `PackingControllerSecurityIT`.
  - `codex review --commit 16b2a2c4`: no regressions found.
- 2026-02-12: `65f53375`
  - Reject JWT authentication for disabled users.
  - Added `AuthDisabledUserTokenIT`.
  - `codex review --commit 65f53375`: review executed; no critical regression reported.

Current Checkpoint
- Config hardening implemented for runtime profiles:
  - `erp-domain/src/main/resources/application.yml`
  - `erp-domain/src/main/resources/application-dev.yml`
  - `erp-domain/src/main/resources/application-prod.yml`
- Coverage added:
  - `erp-domain/src/test/java/com/bigbrightpaints/erp/core/config/ConfigurationSecretsHardeningTest.java`
  - `erp-domain/src/test/java/com/bigbrightpaints/erp/codered/CR_HealthEndpointProdHardeningIT.java`
- Verification command:
  - `mvn -B -ntp -Dtest=ConfigurationSecretsHardeningTest,CR_HealthEndpointProdHardeningIT test`
- Next action:
  - commit this step
  - run `codex review --commit <sha>`
  - continue dealer-role abuse audit
