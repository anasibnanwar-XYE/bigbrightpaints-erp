# RULES

1. checkout a new branch when starting a task
2. complete the task as described; attempt to reduce code, not make more, for older projects
3. run type-check, lint, full test suite (do not delete or skip tests), commit and push
4. update documentation

